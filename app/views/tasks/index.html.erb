<h1><%= params[:status].present? ? params[:status].titleize : "All" %> Tasks</h1>

<%= react_component 'Tasks',
    props: { status: params[:status] } %>

<hr>

<% unless params["status"] == "completed" %>
  <%= render 'form', task: @task %>
<%end%>
